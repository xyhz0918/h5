# AGENTS.md

## 项目身份

这是“HORSH 豪士透明工厂 早餐小 BUG 透明验证”移动端 H5 项目。目标是做一套竖版互动营销体验，让用户从发现早餐困扰、选择早餐小 BUG、生成问题工单、进入透明工厂、完成原料/松软/醒发/焙烤/透明验证等控制舱互动，最后生成早餐透明报告。

默认用中文沟通。涉及代码修改时，先用大白话说明“为什么改、改哪里、风险是什么”，再动手。

## 技术栈与主要文件

- 技术栈：Vite + React + TypeScript + CSS。
- 主入口：[src/App.tsx](C:/Users/XYHZ/Documents/大广赛豪士5/src/App.tsx)
- 主样式：[src/styles.css](C:/Users/XYHZ/Documents/大广赛豪士5/src/styles.css)
- 主要素材目录：[assets](C:/Users/XYHZ/Documents/大广赛豪士5/assets)
- 构建命令：`npm run build`
- 本地预览：通常是 `http://127.0.0.1:5173/`，也可能因端口占用变成 `http://localhost:5180/`，以 dev server 输出为准。

## 协作原则

- 不要随意重构项目结构。这个项目大量视觉和交互微调集中在 `src/App.tsx` 和 `src/styles.css`。
- 不要擅自恢复、删除或覆盖用户已有改动。看到未提交改动时，默认它们是用户或前序迭代留下的。
- 不要做破坏性 git 操作，例如 `git reset --hard`、`git checkout --`。
- 不要提交 git commit，除非用户明确要求。
- 手工改文件优先用 `apply_patch`。
- 修改文件、运行命令、访问外部账号前，简短说明目的和风险。
- 用户调视觉时通常希望快速迭代，回复要短，先落地，再验证。

## 画布与适配

- 设计画布按 `640 x 1030px` 固定比例制作。
- 手机打开时不是物理显示成 640 x 1030，而是这个画布按手机 CSS 可视区等比缩放。
- 不要把核心布局改成随手机高度自由拉伸，否则会和设计稿比例不一致。
- 桌面预览应居中展示手机画布，移动端优先保证首屏比例、按钮可点、文字不溢出。

## 当前页面流程

核心体验按这些页面推进：

1. 首页：检测到一个早餐小 BUG
2. 选择 BUG 页：选择早餐困扰
3. 工单页：生成早餐问题档案，进入豪士透明工厂后台
4. 原料数据舱：拖入原料卡，完成原料接入
5. 松软唤醒舱：按住启动压面机，完成松软唤醒
6. 恒温醒发舱：拖动温湿度滑杆到稳定区
7. 黄金焙香舱：随着温度变化吐司烤色变化，锁定黄金区
8. 透明验证舱：拖动扫描器读取追踪码，完成透明验证
9. 报告页：生成我的早餐透明报告

首页底部仍使用 4 步总流程：`识别困扰 / 数据接入 / 工艺验证 / 生成报告`。

操作舱小流程只用于操作舱相关页面，节点是：`原料 / 松软 / 醒发 / 烘焙 / 验证`。不要把这套小流程放回工单页，工单页应保留 4 步总流程。

## 视觉方向

整体是黑绿色赛博控制台、Matrix 数字雨、透明玻璃 HUD、未来食品工厂风格。关键词：

- HORSH 豪士
- 透明工厂
- 早餐小 BUG
- 工单系统
- 智能制造
- 原料检测
- 松软唤醒
- 恒温醒发
- 黄金焙香
- 透明验证

视觉要商业 H5、科技、游戏化，但不要过度花哨。用户偏好是“深色玻璃 + 克制绿色发光”，不是高饱和刺眼绿色。

## 重要视觉约定

- 核心文字必须保持前端可编辑，不要把大标题、按钮、核心说明文案烘焙进图片。
- 图片主要承担角色、背景、产品、工厂、氛围和 HUD 素材。
- 不要把页面做成普通食品海报，要像可交互系统界面。
- 不要叠太多蓝绿遮罩、扫描线、雾、网格；用户多次反馈“花里胡哨”“要看见工厂/产品本身”。
- 卡片不要过圆，保持科技硬边和约 8px 圆角。
- 文本不能溢出按钮或卡片。

## 首页约定

首页是用户最在意的页面，改动要谨慎。

- 首页主视觉使用分层素材：
  - 光环平台：`assets/images/home/home-platform.webp`
  - 豪小士角色：`assets/images/home/home-mascot.webp`
  - HUD 浮窗：`home-panel-online.webp`、`home-panel-plan.webp`、`home-panel-bread.webp`、`home-panel-scan.webp`、`home-panel-complete.webp`
- 光环必须在人物脚下，不要变成盖在人物背后的大圆盘。
- 光环素材曾由 PNG 压缩为 WebP，要保持视觉质量，不要随意换回大 PNG。
- 五个 HUD 浮窗围绕人物四周，不要挤压主角。
- “验证完成”浮窗不要挪回右侧拥挤区域。
- 首页不再保留“查看透明工厂”按钮。
- 首页警告盒子 `.warning-carousel` 放在标题下方、说明文案上方，里面轮播早餐场景问题。
- 标题使用 `.glitch-title` 轻微 glitch 动画，要有切片错位感，但不要快到看不清。

## 工单页约定

- 工单页是“早餐问题档案接入透明工厂后台”。
- 底部流程应使用原来的 4 步总流程，不要使用操作舱小流程。
- 工厂主体图应作为中层视觉：在背景层之上、模块和按钮之下。
- 工厂图不是新卡片、不是独立展示窗，不要加额外标题牌。
- 工厂需要尽量靠下，不要挡住工单信息和主按钮。
- 工厂图要用正常混合模式，避免被背景、雾、网格染得看不清。

## 原料数据舱约定

- 玩法应以拖拽为主，目标是把原料卡拖入透明搅拌核心。
- 原料卡包括红藜麦、谷朊粉、小麦、鲜酵母等。
- 按钮文案要和拖拽进度一致，例如 `拖入原料数据 0/4`、完成后进入压面。
- 页面底部可以显示操作舱小流程，当前高亮 `原料`。
- 不要让提示弹窗或 toast 挡住核心视觉太久。

## 压面页约定

- 玩法是“按住启动压面机”，不是单纯点击。
- 压面时间不能太长，失败反馈不能过度挫败。
- 已删除重复的“36 米压面进度 DATA TUNNEL 0-36M”卡片；不要再加回。
- 页面底部操作舱小流程当前高亮 `压面`。

## 醒发页约定

- 玩法是拖动温度、湿度滑杆到稳定区。
- 页面要让人一眼知道怎么玩：可用清楚的目标区、拖动提示、达标状态。
- 已删除顶部过长的三步说明条；不要加回。
- 页面底部操作舱小流程当前高亮 `醒发`。

## 焙烤页约定

- 参考图方向是“黄金焙香舱”：烤箱、吐司、温度仪表、目标黄金区。
- 吐司要随着温度实时变化：
  - 未烤过：使用未烤吐司图。
  - 黄金区：色泽自然变金黄。
  - 过烤：使用过烤图。
- 图片必须对齐，不要出现叠图错位。
- 不要添加花哨扫描线、蓝色遮罩、闪烁特效。
- 烤箱里只保留真实烤箱图、吐司变化和必要目标温度牌。
- 火候扫描要有逻辑，黄金区停留感要更明显，不能快到用户来不及操作。
- 页面底部操作舱小流程当前高亮 `烘焙`。

## 透明验证页与报告页约定

- 透明验证页玩法是拖动扫描器读取包装追踪码。
- 页面底部操作舱小流程当前高亮 `验证`。
- 报告页要使用本次真实工单号，不要硬编码旧编号。
- 报告页支持保存、分享；分享不可用时降级为复制文案。
- 不接真实后台、真实直播流、真实购买链路，除非用户明确要求。

## 素材处理

- 通过 `new URL("../assets/...", import.meta.url).href` 引用素材。
- 新增大图优先压缩成 WebP，保持视觉质量，不要明显损伤边缘、透明度或品牌细节。
- 如果新素材是透明 PNG，要检查透明边界和裁切，避免出现整块半透明竖板。
- 品牌和产品素材不可变形。
- 产品主推是豪士藜麦吐司。

## 动画偏好

- 动画要服务任务推进和控制台反馈。
- 可以使用轻微呼吸、扫描光、数据跳动、按钮反馈。
- 避免大面积脉冲环扩散，用户已经要求删过中央脉冲环。
- 避免影响真实产品/工厂可见性的遮罩、闪烁和过强特效。

## 验证清单

每次重要修改后至少做：

- `npm run build`
- 刷新本地预览
- 用浏览器截图检查当前改动页面

涉及流程时额外检查：

- 首页按钮进入选择页
- 未选 BUG 的限制逻辑
- 100 字输入限制
- 工单页数据带入
- 原料拖拽/录入完成
- 压面按住启动
- 醒发滑杆进入稳定区
- 焙烤黄金区锁定
- 透明验证码扫描
- 报告保存/分享降级

视觉改动成功标志：

- 首页一眼看到品牌、豪小士、主标题、主按钮。
- 光环在人物脚下，浮窗围绕人物。
- 工厂页能看见工厂，不被遮罩和网格压没。
- 黑绿科技感统一，但不刺眼、不发灰。
- 大标题和核心文案都是前端可编辑文字。

## CodeGraph

本项目配置了 CodeGraph MCP。做结构性问题时优先使用 CodeGraph，而不是先 grep。

- 查符号定义：`codegraph_search`
- 查调用关系：`codegraph_callers` / `codegraph_callees`
- 查任务上下文：`codegraph_context`
- 查看相关源码：`codegraph_explore`
- 查看文件结构：`codegraph_files`
- 分析影响范围：`codegraph_impact`

文字内容、样式类名、文案字符串等字面搜索仍可使用 `rg`。

## 回答风格

- 默认中文。
- 先说目的和风险，再说具体改动。
- 视觉迭代时不要写太长解释，快速改、快速验证。
- 最终回复简短说明：改了哪里、验证结果是什么、还剩什么风险。
## 线上构建与部署保护

- 默认禁止触发线上构建、部署、重部署、回滚或生产发布。
- 未经用户明确确认，不要运行会消耗平台额度的操作，包括 `git push`、`vercel deploy`、`vercel redeploy`、`vercel promote`、`netlify deploy --trigger`、`netlify deploy --prod`、后台 Deploy/Redeploy 按钮、Deploy Hook。
- 如确实需要上线，必须先说明：会触发哪个平台、可能消耗什么额度、影响哪个域名、失败如何回滚，并等待用户明确说“确认部署/确认上线/可以推送”。
- Netlify 当前存在 credits/usage 风险，默认只读检查，不主动触发 Netlify 构建或生产部署。
- 本地验证命令如 `npm run build`、`npm run smoke:mobile` 不消耗 Vercel/Netlify 额度，但运行前也要说明目的和风险。
- DNS、域名、环境变量、账号计费、删除站点/团队等后台操作，都必须先说明风险并等待用户确认。
