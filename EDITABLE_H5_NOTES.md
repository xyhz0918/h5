# 豪士透明工厂 H5 可编辑版说明

## 大白话说明

当前版本已经不是整张效果图当底图的演示版，而是 React + CSS 真实页面版。

页面里的标题、按钮、流程、卡片、工单字段、检测状态、报告文案、推荐早餐和数据仪表盘，都可以直接在 `src/App.tsx` 里修改。页面样式、动画、布局和黑绿 HUD 效果在 `src/styles.css` 里修改。

## 成功标志

- 打开 `http://127.0.0.1:5173/` 能看到首页。
- 从首页点击到报告页，流程完整可用。
- 浏览器 DOM 能读到真实中文文字，而不是只有一张图片。
- `npm run build` 可以通过。

## 主要文件

- `src/App.tsx`：多页面内容、状态、交互和报告生成逻辑。
- `src/styles.css`：全部视觉样式、动效、手机比例和响应式适配。
- `assets/images`：背景、角色、徽章等素材。
- `assets/product/quinoa-toast`：豪士藜麦吐司产品素材。

## 当前取舍

- 优点：文案和结构全部可编辑，后续改活动文案、按钮、工单和报告不需要重新出图。
- 取舍：不再追求与 8 张效果图逐像素完全一致，视觉接近效果图风格，但由前端组件重新搭建。
- 信息密集页在小屏幕上允许纵向滚动，避免文字挤压或重叠。

## 游玩逻辑

用户必须按任务顺序推进，不能直接跳关：

1. 首页点击“送进豪士透明工厂”。
2. 选择一个早餐小 BUG，点击按钮生成早餐问题档案。
3. 工单页点击“进入原料数据舱”，进入透明工厂流程。
4. 原料数据舱拖入原料卡，完成原料接入。
5. 松软唤醒舱按住启动压面机，完成松软唤醒。
6. 恒温醒发舱拖动温湿度滑杆到稳定区。
7. 黄金焙香舱在黄金区锁定火候。
8. 透明验证舱拖动扫描器读取包装追踪码。
9. 报告页生成“我的早餐透明报告”。

当前限制：

- 未选择早餐小 BUG 时不能直接生成工单。
- 操作舱小流程按原料、松软、醒发、烘焙、验证顺序推进。

## 已验证

- `npm run build` 通过。
- 浏览器从首页跑到报告页通过。
- `375x667`、`390x844`、`430x932` 三种尺寸无横向溢出。
